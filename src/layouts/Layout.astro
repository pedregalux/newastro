---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { ClientRouter } from 'astro:transitions';

interface Props {
	title?: string;
	description?: string;
}

const { title = 'Pedregalux web log', description = 'Internet ya no es el mismo' } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial screen scale=1 0 auto 0" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/png" href="/favicon.png" />
		<meta name="description" content={description} />
		<title>{title}</title>
		<ClientRouter transition:persist />
	</head>
	<body class="bg-fixed bg-gradient-to-br from-zinc-950 via-zinc-800 to-zinc-950">
		<Header />
		<slot />
		<Footer />
	</body>
</html>
<style is:global>
	:root {
		color-scheme: dark;		
	}
	html {
		font-family: system-ui, sans-serif;
		background: #13151a;
	}
	body {
		display: flex;
		flex-direction: column;
		justify-content: center;
		font-size: 1rem;
		line-height: 2.0;
	}
	nav {
		font-family: Ubuntu, sans-serif;
		font-weight: 200;
		font-size: 1rem;
	}
	nav a {
		color: #ffffff;
		text-decoration: underline #7dd3fc;
		text-decoration-style: dotted;
		text-underline-offset: 5px;
	}
	.cuerpo a {
		color: #7dd3fc;
		text-decoration: underline grey;
		text-decoration-style: dotted;
		text-underline-offset: 5px;
	}
	.cuerpo a:hover {
		color: #7dd3fc;
	}
	.nonunder {
		text-decoration: none;
	}
	.cuerpo {
		font-size: 1.1rem;
		color: #d6d3d1;
		line-height: 1.8rem;
		font-family: 'DM Mono', monospace;
	}
	.sidebar {
		font-size: 1rem;
		line-height: 1.8rem;
		color: #d4d4d8;
		font-family: 'DM Mono', monospace;
	}
	.sidebar a {
		color: #ffffff;
		text-decoration: underline #7dd3fc;
		text-decoration-style: dotted;
		text-underline-offset: 5px;
	}
	h1 {
		color: #7dd3fc;
		font-family: Ubuntu, sans-serif;
		font-weight: 400;
		font-size: 2.8rem;
		line-height: 3.5;
	}
	p {
		margin-bottom: 1.2rem;
		font-family: DM Mono, monospace;
	}
	h3 {
		color: #7dd3fc;
		font-family: Ubuntu, sans-serif;
		font-weight: 400;
		font-size: 1.8rem;
		line-height: 3;
	}
	h2 {
		color: #7dd3fc;
		font-family: Ubuntu, sans-serif;
		font-weight: 400;
		font-size: 2rem;
		line-height: 3.5;
	}
	
</style>
